const a = 4;
const b = 6;

function add(x, y) {
  return x + y;
}

function sub(x, y) {
  return x - y;
}

function mult(x, y) {
  return x * y;
}

function div(x, y) {
  return x / y;
}
function mod(x, y) {
  return x % y;
}

console.log(add(a, b));
console.log(sub(a, b));
console.log(mult(a, b));
console.log(div(a, b));
console.log(mod(a, b));
